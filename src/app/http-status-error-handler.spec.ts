import { HttpStatusErrorHandler } from './http-status-error-handler';

describe('HttpStatusErrorHandler', () => {
  it('should create an instance', () => {
    expect(new HttpStatusErrorHandler()).toBeTruthy();
  });
});
