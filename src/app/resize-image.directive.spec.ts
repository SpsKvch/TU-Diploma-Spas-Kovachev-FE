import { ResizeImageDirective } from './resize-image.directive';

describe('ResizeImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeImageDirective();
    expect(directive).toBeTruthy();
  });
});
